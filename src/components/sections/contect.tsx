import { component$ } from "@builder.io/qwik";

export default component$(() => {
    return (
        <section id="contact" class="flex flex-col min-h-screen items-center">
        <h2 class="text-4xl font-semibold p-3 bg-white w-full text-center text-blue-950">Contact</h2>
      </section>
    )
})